# 通宵批次报告（2026-09-23 01:00 – 08:30，全部 RIS）

五项全部推进，**四项有结论、一项部分完成**。最重要的一条在第 4 节：扰动引擎当前**不可用**，
而且是被一个专门设计来抓它的对照抓到的。

所有脚本入口都在 `figure2_eval/ris/figure2.sbatch` 的 `JOB=` 里；结果 JSON 在
`figure2_eval/results/`；逐行数字在 `figure2_eval/README.md` 第 10 节及以后。

---

## 0. 一页纸

| # | 项目 | 状态 | 一句话结论 |
|---|---|---|---|
| 1 | 位置编码 + 噪声地板 | ✅ 完成 | 只有**变化率定标**和 **LM val loss** 站得住；我之前报的两条改进**是噪声** |
| 5 | 真 test split | ✅ 完成 | 数据+模型+held-out 评估都有了；**val 选择带来的乐观度很小**（0.79→0.80） |
| 4 | 扰动评估套件 | ✅ 完成 | **零扰动对照未过**：改队列标签对 AD 的影响 ≥ 一个 APOE ε4 拷贝 |
| 2 | 多任务判别头 `aux_head` | ✅ 完成 | MMSE `skill vs W` −0.069 → **−0.008**，六次训练里第一次追平单访视 logistic |
| 3 | 拆"何时来访/发什么" `visit_heads` | ✅ 完成 | 表征变好（emb 均值 0.789→**0.809**，首次稳超 raw），但伤 MMSE 方向判别 |

---

## 1. 噪声地板（#1）—— 先读这个，它决定别的能不能读

3 个 seed × 2 配置（`nodedup` / `nodedup-pos`），其余一字不改。**组内 std = 单次训练的噪声。**

| 指标 | 噪声尺度 |
|---|---|
| `skill vs W`（MMSE / cogn） | **±0.05 / ±0.13** |
| 条件方向 top-1 | **±0.04** |
| 直接头 per-row AUC | **±0.02** |
| 变化率 | ±0.02 |

**位置编码站得住的只有两条**：

1. **MMSE 变化率定标**：0.228 [0.208,0.247] → **0.185 [0.177,0.197]**，实测 0.197，两组范围不重叠。
2. **LM val loss**：8.468/8.476/8.502 对 8.445/8.427/8.465，**三对三不重叠**。

**我之前报过、现在作废的**（都落在噪声里）：
- ~~"skill vs W −0.069 → −0.020，几乎补平"~~ → std ±0.05，重叠
- ~~"AD 直接头 0.818 → 0.834"~~ → 0.804±0.023 → 0.812±0.019，重叠

**核心问题没解决**：`skill vs W` 在 6 次训练里**全为负**。"整条纵向历史没超过一个只看当次访视的
logistic"这一条，位置编码没有改变。根因假设只得到部分支持。

---

## 2. 真 test split（#5）—— 好消息

`build.py --split 0.8,0.1,0.1`，默认 `0.9,0.1,0` **md5 逐字节复现交付版**（本地+RIS 各验一次）。
关键性质成立：**val 与两分版完全相同、train 是两分版 train 的前缀子集**（沿用同一次
`RNG.shuffle`），所以三分结果可与已有结果逐人对读。train 3542 / val 443 / **test 443**。

在 **test.bin**（从未参与 ckpt 选择）上的直接头判别力：

| | raw（LR） | emb（直接头） |
|---|---|---|
| panel a 8 行中位 | 0.797 | **0.813**（nd3w）/ 0.800（pos3w） |
| AD diagnosis | 0.786 | **0.808 / 0.835** |
| Death | 0.809 | 0.821 / 0.794 |

**结论**：和 val 上的数字（0.789–0.798）几乎一样。也就是说 §3.3 担心的"val 既选 ckpt 又报数"
带来的乐观度**很小**，前面所有结论不因此失效。这是个干净的好消息。

---

## 3. 两个架构改动（#2 / #3）—— 代码可信，评估未完

`model.py` / `train.py` / `utils.py` 加了两个**独立且默认关闭**的 flag：
`aux_head`（+`aux_lambda`）与 `visit_heads`（`time_head` 标量访视速率 + `size_head` 访视大小）。
配套 `make_aux_labels.py` 生成与 `.bin` 逐行对齐的删失感知标签（3 终点 × 5 horizon = 15 列，
**逐访视**监督，每人约 7 个点而不是 1 个）。

**验证（都在 RIS 上跑的）**：
- `test_getbatch_equiv.py`：默认路径与改动前 **逐位一致**，含 `lifestyle_augmentations` 开启的情形。
- `test_heads_shapes.py`：**86 项全过**，包括"抖动过的 token 不参与判别头监督"和"形状不符必须抛
  AssertionError 而不是静默 reshape"。

两个模型都训完了（各约 3.5 分钟），评估也赶上了。**判读时请对照第 1 节的噪声地板**
（MMSE skill ±0.05 / cogn skill ±0.13 / 条件方向 ±0.04 / 直接头均值 ±0.01）；**都是单 seed**。

| | base | **aux** | **vh** | W 单访视 |
|---|---|---|---|---|
| MMSE `skill vs W` | −0.069 | **−0.008** ⬆ | −0.131 ⬇ | 0 |
| MMSE 条件方向 top-1 | 0.741 | 0.763 | **0.661** ⬇⬇ | 0.765 |
| MMSE 预测变化率（实测 .197） | 0.231 | 0.228 | 0.264 | 0.192 |
| cogn `skill vs W` | −0.208 | −0.259 | **−0.046** ⬆ | 0 |
| cogn 预测变化率（实测 .165） | 0.339 | 0.370 | **0.255** ⬆ | 0.166 |
| 直接头 emb 中位 / 均值 | 0.777 / 0.789 | 0.788 / 0.793 | **0.800 / 0.809** ⬆ | raw 0.792 |
| 直接头 AD 5y | 0.818 | 0.794 | 0.823 | raw 0.794 |

**超过噪声地板的三条**：

1. **`aux` 把 MMSE 的 `skill vs W` 做到 −0.008** —— 从 −0.069 起，Δ=0.061 略超 ±0.05。
   这是**六次训练里第一次基本追平单访视 logistic**。方向对，但只差一点点就落回噪声，需要复现。
2. **`vh` 明显伤了 MMSE 的条件方向**：0.741 → 0.661，Δ=−0.080 是噪声的 2 倍。
3. **`vh` 让表征变好**：直接头 emb 均值 0.789 → **0.809**（Δ=0.020，噪声 ±0.01 的 2 倍），
   且**高于 raw 的 0.792** —— 这是表征第一次稳稳超过原始特征。cogn 的 `skill vs W`
   −0.208 → −0.046 也超过 ±0.13。

**怎么解读这个分裂**：`vh` 把时间头从"全词表 logsumexp"换成了专用标量，等于**把"什么时候"
的压力从 token 分布上卸下来**。代价是 MMSE 的逐 token 方向判别变差（条件方向 0.661），
收益是表征更干净、cogn 的变化率定标从 2.06× 收到 1.55×。**这正好是拆分该有的样子**，
但也说明单靠 `visit_heads` 不够——它需要和 `aux`（把判别压力也卸到专用头上）一起用。
**下一步最该跑的单个实验：`aux_head=True` + `visit_heads=True` 同时打开，3 个 seed。**

⚠️ **别拿它们的 val loss 横向比**：`aux` 的总损失多了 `aux_lambda*loss_aux`，`vh` 的 `loss_dt`
换了定义还多了 `loss_size`，8.68 / 12.63 和基线的 8.47 **不是同一个量**。

---

## 4. 扰动评估套件（#4）—— 最重要的一节

`perturbation_eval.py`（`JOB=pe`）。全量：val 全部病人，`n_mc=200`，两个模型，1 小时 26 分。

四类测试：MC 噪声地板 / APOE 剂量反应 / **零扰动对照** / 注入对照 + 靶点-脱靶。

### 4.1 零扰动对照没过 —— 这一条否决其余所有数字

把 `STUDY_ROS` ↔ `STUDY_MAP` 互换（队列标签，**不该有任何因果效应**），看单步 AD 速率比：

| 扰动 | nodedup | dedup |
|---|---|---|
| APOE ε4 = 0 | 1.0138 [1.0061,1.0214] | 0.9944 |
| APOE ε4 = 1 | 1.0189 [1.0105,1.0279] | 1.0053 |
| APOE ε4 = 2 | **1.0544** [1.0377,1.0705] | 0.9963 |
| **零扰动（队列标签）** | **1.0229** [1.0150,1.0317] | **0.9795** [0.9770,0.9821] |

**在 `nodedup` 上，一个毫无因果意义的队列标签互换，对 AD 速率的影响（2.3%）比一个 ε4 拷贝
（1.9%）还大**，只比两个拷贝（5.4%）小。在 `dedup` 上更糟：APOE **完全没有剂量反应**
（0.994/1.005/0.996，不单调），而零扰动动了 2.1%。

### 4.2 注入对照也没过

注入 `ANTIHYP_ON`（开始降压）与反方向的 `ANTIHYP_OFF`（停药）：

| | nodedup | dedup |
|---|---|---|
| `ANTIHYP_ON` | 0.9906 | 1.1708 |
| `ANTIHYP_OFF`（反方向） | 0.9344 | 0.7772 |

`nodedup` 上两个**相反**的干预给出**同方向**的效应（都 <1），ON/OFF 比值 1.047 ≈ 1
→ 测到的是"前缀多了一个 token、后面位置整体后移"这个**插入假象**，不是药效。

### 4.3 5 年 rollout 层面：全部埋在 MC 噪声里

零扰动的 5 年 AD 风险 Δ：`nodedup` +0.0080 [−0.0038,+0.0199]、`dedup` −0.0007 [−0.0109,+0.0098]
—— **CI 都含 0**，很好。但同一批人的**逐人** mean|Δ| 是 **0.0815 / 0.0609**，
也就是说 n_mc=200 下单个病人的反事实风险差有 ±0.06–0.08 的纯采样噪声。
**APOE 这种量级的效应在个体层面根本分辨不出来。**

### 4.4 所以怎么办

按优先级：

1. **先修零扰动**。模型对 statics 的敏感度不该来自队列标签。可查的方向：
   队列标签和 APOE/教育/种族在基线访视同处一个位置块，`token_dropout=0.0` 意味着模型可以自由
   依赖任何一个；先试把队列 token 移出可扰动集合（它本来就不是干预目标），再看 APOE 的剂量反应
   是否变干净。
2. **注入类扰动需要专门的基线**。任何"加一个 token"的干预都必须和一个语义相反或中性的
   注入对照配对报告，否则报的是插入假象。这条已经写进脚本，以后不会再漏。
3. **个体层面的反事实需要 n_mc ≫ 200**。地板按 1/√n_mc 收缩，要把 0.08 压到 0.02 需要
   n_mc ≈ 3200，单模型成本约 20 小时。**或者**只报群体层面的平均 Δ（CI 已经足够窄），
   放弃"这个人吃了药会怎样"的个体叙事。

---

## 5. 没做到的 / 欠账

- **`aux` + `vh` 同时打开**没跑（这是上面推出来的最优先实验）。两个改动都是单 seed，
  按第 1 节的地板，只有三条差异超过噪声。
- **V2 相对时间 bias** 没做（`age_q − age_k` 加到 attention logits）。V1 只给了序号，
  而"多久以前"比"第几个"更接近临床语义。这是位置编码那条线的下一步。
- **三分 split 只重训了 `nodedup` 和 `nodedup-pos` 两档**，`dedup` / `fullvisit` 没重训。
- **`STROKE`/`DEPRESSION` 逐访视发射**（`--per-visit-events`，+0.47% token）按你的决定没重跑。
- 通宵 workflow 的 arch 分支**审查 agent 卡死**，那份代码的对抗性审查是我自己补的
  （等价性 + 形状），不如另外两份彻底。

---

## 6. 我认为醒来后最该做的一件事

**修零扰动，在此之前不要引用任何扰动数字。** 判别力那条线已经被四个方向证明撞墙
（第 8、9 节），项目的价值主张只能落在反事实上；而现在的测量说反事实**还不可信**。
这不是坏消息——是这套评估第一次让这件事可被发现，而且它在一小时半内就给出了判决。
