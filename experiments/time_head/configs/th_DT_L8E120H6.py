# DT_L8E120H6 -- 1,412,160 params (1,412,160 trunk + 0 added by this arm)
#
# fixed timing target on the shape the sweep recommends
#
# GENERATED by experiments/time_head/gen.py -- DO NOT EDIT BY HAND.
#
# Everything below this header is experiments/capacity/configs/cap_L8E120H6.py, read
# verbatim, with these lines appended at the end and NOTHING else changed:
#     dt_target = 'next_event'
# So this arm and the capacity sweep's `L8E120H6` arm differ in those keys only -- same
# dataset, same cohort filter, same schedule, same eval cadence, same seeds. The control is
# NOT re-run; it is experiments/capacity/runs/L8E120H6_s<seed>. See arms.py for why that
# pairing is exact rather than approximate.
#
# out_dir and seed are supplied on the command line by the runner, not set here.
dataset = "nacc-dedup-s42"

vocab_size = 111
block_size = 96

t_min = 365.25 / 12

ignore_tokens = list(range(22))

# --- THE swept variables ---
n_layer = 8
n_head = 6
n_embd = 120

# --- frozen schedule ---
max_iters = 5000
batch_size = 128
learning_rate = 2e-3
lr_decay_iters = 5000
min_lr = 2e-4
warmup_iters = 500
beta2 = 0.99
dropout = 0.0
token_dropout = 0.0
weight_decay = 0.2
grad_clip = 1.0

eval_interval = 100
eval_iters = 200

# cohort filter left at train.py's defaults (4 / 2) -- the same 16,262-patient population
# the delivered model was fitted on. Not a swept variable.

wandb_project = "ad-projection"

# --- THE experimental variable(s) ----------------------------------------------------------
# fixed timing target on the shape the sweep recommends
dt_target = 'next_event'