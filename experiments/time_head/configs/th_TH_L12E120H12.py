# TH_L12E120H12 -- 2,104,441 params (2,104,320 trunk + 121 added by this arm)
#
# time head on the DELIVERED architecture -- reads straight against the capacity table's reference arm
#
# GENERATED by experiments/time_head/gen.py -- DO NOT EDIT BY HAND.
#
# Everything below this header is experiments/capacity/configs/cap_L12E120H12.py, read
# verbatim, with these lines appended at the end and NOTHING else changed:
#     time_head = True
# So this arm and the capacity sweep's `L12E120H12` arm differ in those keys only -- same
# dataset, same cohort filter, same schedule, same eval cadence, same seeds. The control is
# NOT re-run; it is experiments/capacity/runs/L12E120H12_s<seed>. See arms.py for why that
# pairing is exact rather than approximate.
#
# out_dir and seed are supplied on the command line by the runner, not set here.
dataset = "nacc-dedup-s42"

vocab_size = 111
block_size = 96

t_min = 365.25 / 12

ignore_tokens = list(range(22))

# --- THE swept variables ---
n_layer = 12
n_head = 12
n_embd = 120

# --- frozen schedule ---
max_iters = 5000
batch_size = 128
learning_rate = 2e-3
lr_decay_iters = 5000
min_lr = 2e-4
warmup_iters = 500
beta2 = 0.99
dropout = 0.0
token_dropout = 0.0
weight_decay = 0.2
grad_clip = 1.0

eval_interval = 100
eval_iters = 200

# cohort filter left at train.py's defaults (4 / 2) -- the same 16,262-patient population
# the delivered model was fitted on. Not a swept variable.

wandb_project = "ad-projection"

# --- THE experimental variable(s) ----------------------------------------------------------
# time head on the DELIVERED architecture -- reads straight against the capacity table's reference arm
time_head = True